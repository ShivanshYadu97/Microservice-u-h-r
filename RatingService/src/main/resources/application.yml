server:
  port: 8083

spring:
  application:
    name: RATING-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/ratingmicro
    username: postgres
    password: shivansh

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/back-microservice-h-u-r

eureka:
  instance:
    prefer-ip-address: true

  client:
    fetch-registry: true
    register-with-eureka: true

    service-url:
      defaultZone: http://localhost:8761/eureka